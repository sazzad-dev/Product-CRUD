import { colors } from '@mui/material';

const colorConfigs = {
  sidebar: {
    bg: '#152B40',
    color: '#ffffff',
    hoverBg: '#1e293a',
    activeHoverBg: '#eaeaea',
    activeBg: '#fff',
    activeColor: '#000',
  },
  topbar: {
    bg: '#fff',
    color: '#000',
  },
  mainBg: colors.grey['100'],
};

export default colorConfigs;
